---
- hosts: localhost
  gather_facts: no
  vars:
    gce_instance:
      name: "{{ lookup('env', 'GCE_INSTANCE') }}"
      state: started
      zone: "us-central1-a"
    gce_disk:
    gce_network:
    gce_ip:
  roles:
    - gce